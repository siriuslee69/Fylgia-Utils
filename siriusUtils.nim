import 
    src/
        [
        io/responses,
        math/even,
        math/weights,
        math/genetic,
        math/boolean,
        strutils/words,
        streams/write,
        dirs/files
        ]

export responses, boolean, weights, genetic, words, write, even, files


    
